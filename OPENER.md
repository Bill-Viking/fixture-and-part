lane: F&P
written: 2026-09-23T12:47-07:00
by: ACT — F&P: the next arc (Mac) · paused on Bill's word through the Desk
on: MAC
head: f2e55d2
status_updated: 2026-09-23T12:47-07:00
where: arc picked (the window for a novice); brief + words on main; words await go/strike
next: bill
depends_on: nothing
open_in: ~/Projects/fixture-and-part on either machine (folder name `fixture-and-part`, reuse) · fresh Claude session titled `ACT — F&P: the window for a novice`
model: FABLE · EXTRA for the words ruling, folding any strikes, and verifying the build (judgment); the build itself is an OPUS · HIGH subagent in its own worktree, named in the Agent call.
judgment: folding Bill's strikes into WORDS.md and explainers.js; verifying the builder's numbers, code and stills against the brief; the one still Bill judges; the deploy call. The build between is mechanical.
---
read the project status/memory first. You are the F&P lane (FIXTURE⊕PART). DEPENDS ON: nothing — this lane rides GitHub: `git pull` in the project folder first, and confirm main is at the head pinned above (or later, by docs commits only).

Ritual in one line of receipts: clock (`date`); `sync.py pull` from the orchestrator-sync folder (`python3` on the Mac, `python` on the Dell); `git pull` and `git log --oneline -3` in the project; one-writer check (`tools/who-is-live.py --pen --lane "F&P"` in the hub folder, and no other ACT — F&P session anywhere); stamp and claim the lane (`tools/i-am.py`, `tools/claim.py --claim "F&P"`); title yourself `ACT — F&P: the window for a novice`. Read STATUS.md ## Now (the top paragraph is this pause), then `design/window-for-a-novice/WORDS.md` and `BUILD-BRIEF.md` in full — they are the arc. On the Dell, a deploy builds through PowerShell (## Important, first bullet); on the Mac the recipe is the one in the project-state memory's Phase 3A paragraph.

Deliver WHERE WE ARE first, plain words, Sal register:
- Arc goal: the window for a novice — instrument E's byte window in section 01 gets its labels and its plain words, every one read from the real file. Bill picked it 2026-09-23 morning.
- Already happened: ✅ the pick filed · ✅ the model file read and every column of the word table measured — slot 36 holds nearly the same number for every word, slot 6 runs low for nearly all of them, both inside the window · ✅ the brief and the words written and committed on main e454bce.
- Right now: nothing is built. Tree clean, main = live page (189ed44 shipped; the commits since are docs). Paused 2026-09-23 12:5x so Bill could spend the day on Exocortex.
- Bill's next move: ONE ruling, asked first and before anything else — go or strike on the words. Put them in front of him exactly like this, no preamble:
  · above the window: `the word table (transformer.wte) · words 0–23 of 50,257 · slots 0–63 of 768 · one byte per number`
  · left of each row: the piece itself (`! " # $ …`); above the columns: slot numbers every eighth, and `6` and `36` lit with a `▲`
  · the key under the window: a strip painted by the cells' own shading, ticked `0 · 0.05 · 0.2 · 0.5 · 1.6`, then `shade = size of the number, either sign · dark is near zero`
  · nothing picked: `click a square, or use the arrow keys inside the window, to read one number` and under it `two slots stand out: 36 holds nearly the same number for every word · 6 runs low for nearly all of them`
  · a square picked: `the word "2" · slot 44 · stored as the byte 141 · means +0.16`, today's arithmetic line kept quieter underneath; in slot 36 the line ends `· nearly the same for every word`
  · the "?" explainer retitled `what the window shows`, body rewritten in the same words (WORDS §6, full text there)
- Machine's next move, after go: fold any strike into WORDS.md and write §6 into `src/content/explainers.js` (`fileBlob`), commit on main; start an OPUS · HIGH builder with the Agent tool, `isolation: "worktree"`, its prompt = "read design/window-for-a-novice/BUILD-BRIEF.md and WORDS.md and do what they say; branch `window-for-a-novice` off main <head>" (the model file: re-download via the script's default URL, 83 MB, sha-checked, or copy from a scratch path if one still exists); when it returns, verify first-hand — lint, build, `git diff main --stat` scope, the fileFacts SAME/NEW table, the standout rule's output (36 same, 6 low, eight table-wide), the CLS zeros and the declared static height change, the viewBox trio, the stills — then show Bill ONE still of section 01 at 1280 and ask "ship it?"; merge fast-forward, deploy, verify live with a cache-busted fetch, record every number in STATUS ## Important, on his word only.

Three standing cautions: the words are Bill's — copy them, never improve them; price his side at his rate — one ruling per ask with the recommendation first, a still over a walk; and the harness traps in ## Important (one browser at a time when scoring CLS; reduced motion for a CDP harness with instrument F on screen; a branch's dev server from its own worktree, never the preview tool's primary checkout).

STOP for Bill's go on the words before writing anything to src. Close per the ritual: STATUS ## Now updated, OPENER.md rewritten, commit + push, release the claim, stamp off, retitle ARCH.
