# STATUS — fixture-and-part
<!-- first drafted 2026-08-20; edit freely — the board reads this file -->

## Now
"The Fixture and the Part" — an engineering-drawing-styled interactive guide to transformer internals (React/Vite), built from a static essay with six live instruments (the file, tokenizer, the forward pass drawn live, forward-pass/KV stepper, attention inspector, glass pass). Phase 1 is complete and working: every number is an illustrative heuristic (`src/lib/toyModel.js`), clearly labelled as such, no ML dependency. Phase 2 (real `distilgpt2` via transformers.js) and Phase 3A (the glass pass: per-layer residual stream + logit lens, Instrument D) are verified, merged and deployed, as is Instrument E "the file" (section 01: the real ONNX file — manifest, byte bar, raw-byte blob, byte-exact bell curve, sha-checked re-read; shipped precompute so it shows real facts before any download). The light-theme pass is in progress on the `paper` branch: the page moves off the dark palette onto a white page ground ("Gallery white" — near-white ground, near-black ink, the three role colours at full strength, chosen 2026-08-22 after a cream version was rejected as too beige), dense data fields go onto dark screens set into it, the design tokens are renamed for their colour-law roles, and the copy is rewritten in plain words — including the two moments a reader got stuck on, which are now answered on the page (which sentence every instrument is reading, and what happens to the file when the text changes). Arc 2b shipped 2026-08-23: a decoding control in instrument B (greedy or a seeded sampled draw, sampled by default, because greedy on a six-block model loops within a few tokens — the greedy path itself is unchanged and verified byte-identical to main), and Instrument F "the forward pass, live" — the whole of distilgpt2 drawn once in section 02, its shapes and byte counts read out of the file's own manifest, with the reader's sentence running down it as one amber column per token, a node at each of the seven residual stops sized by the real ‖residual‖ there, attention threads and twelve lit head squares in the selected block, and lettered windows onto A–E. This is what Bill asked for when he said "the neural net is static — I thought we'd see the weight activations as we ran it". Arc 3 "the waterfall" also shipped 2026-08-23: the metaphor is that the file is cut rock training carved and inference never touches, and the activations are water falling through it. Every steel box now wears its own tensor as a faint 48×12 whole-tensor thumbnail read out of the real file at build time — real in both modes — which glints amber as the water crosses it; the selected token's residual falls down the drawing as a 768-cell amber strip, one cell per number, morphing at each of the seven stops to that stop's real values and parking wherever a node is clicked; the columns are finally named on the drawing itself; the logit lens whispers what it hears at each depth as the water passes it; and the last stream lands at the unembedding as a splash of the real next-word probabilities with the sampler's actual pick marked. Next: Arc 4 "melding streams" — Bill's verdict on shipped F is that the engineering drawing won the fight with the waterfall, and his reference image sets the new direction: one continuous fall in a tall dark canyon, attention as strands that meld between streams mid-fall, the machinery receding into the canyon walls as faint etchings from the real tensor bytes. Three static comps are being built for him to pick from before any production work. Arc 4 "the memory room" SHIPPED 2026-08-25 (built and independently verified 2026-08-24, fix pass same day, full-bleed on Bill's ruling, ff-merged f3b5eee and deployed): instrument F is repainted to the comp-a3 design and made live, taking the full sheet at 1280. Six full-width walls, one per block, every cell of them one real i8 byte of that block's own `attn.c_attn.weight` window read out of the file — real in both modes and never rebuilt by a keystroke; the reader's sentence falls through all six as one granular stream per token, its width, light and grain density the real ‖residual‖ on one log law and its texture the 768 dimensions binned into filaments; the last token is the hero and clicking any token re-aims everything at it; each block's transfers into the hero are drawn from the head that looks furthest from the first token and from itself, with a green key at the source, an amber carrier whose width is the real weight, and a bloom where the hero takes it in; a block that draws nothing says so in place with the number that made it a near miss; and the landing stays the last position's alone — it is the last position's stream that reaches the aperture, hero or not, dimmed and said when an earlier token is the hero. Arc 5 "the slow pass" is BUILT on branch `slow-pass` and self-verified 2026-08-26, not merged and not deployed: the 1.1-second replay is replaced by a narrated run — 35 stops on the default sentence in real mode, 1 min 48 s at 1× (19 stops and 66 s illustrative), each stop a line of plain words with this pass's own numbers in it, built by the new `src/lib/tour.js`, which is also the single source of the sentences the sheet says when a reader clicks something; a docent bar sticky over the drawing carries play/pause, step either way, 1×/2×/4× and the caption in a reserved box; a stop moves the picture by setting five custom properties on the screen, so a whole tour writes twelve attributes into the SVG and adds or removes no node at all; left alone for twenty seconds — in view, tab in front, reduced motion off — the sheet runs itself at 60.0 fps with zero long frames in a minute; reduced motion is a stepper rather than a shortened animation; and nothing on the sheet is dead any more — a carrier, a landing ray, a wall cell and both plates each answer a click or a keypress with plain words, a wall cell turning its own byte back into a weight with the file's own scale. Arc 5's FIX PASS landed 2026-09-01 on the same branch (eight commits, tree clean, still not merged and not deployed): Bill ruled on the three motion studies — (a) measured docent wins — so its numbers become the drawing's only ones and the two losers, the `?motion=` switch and everything that existed to choose between tempos are gone; the five findings from the independent verification are answered; and three "say it where the eye is" findings from Bill's own live review are folded in — an answer now opens as a card ON the drawing beside the mark it is about (hovering a wall cell, or clicking a cell, a carrier, a ray or either plate), the docent's line stands beside whatever the tour is talking about, the drawing finally says fixture and part in the essay's own words, and instrument E's byte bar says the one thing it never said, which is that a piece of it is a button. Tabled behind that: 3B "head room" (all-heads grid), 3C "flow threads" (attention arcs during generation).

## To do
- [x] finish verifying Phase 2's real-distilgpt2 mode — verified 2026-08-20 (build/chunking, real BPE ids, embeddings cross-checked against the published fp32 weights, real attention with working layer/head selectors, RUN pacing fix, mode toggle both ways, zero CLS, 390px, reduced-motion, failure path with muted fallback)
- [x] Phase 2 merged into main 2026-08-20
- [x] Phase 3A "glass pass" (Instrument D: residual stream + logit lens, polish pass, ghost callout) merged to main and deployed 2026-08-21
- [x] Instrument E "the file" (section 01: the real decoder_model_quantized.onnx — tensor manifest, byte bar, raw-byte blob, byte-exact bell curve, sha-checked re-read) built, independently verified, merged to main and deployed 2026-08-22
- [x] light-theme pass on branch `paper`: "Gallery white" tokens, ink screens, role-named tokens, a readable byte window in instrument E, and the plain-words rewrite — built 2026-08-22, awaiting review — merged to main and deployed 2026-08-22
- [x] arc 2b on branch `forward-pass`: sampled decoding in instrument B, and Instrument F "the forward pass, live" (section 02, FIG.3 — the figures below it renumber: E=1, A=2, F=3, B=4, C=5, D=6) — built and self-verified 2026-08-23, awaiting review; not merged, not deployed
- [x] arc 3 "the waterfall" on branch `waterfall` (off 2b): build-time whole-tensor thumbnails in `fileFacts.json`, textured steel boxes, the travelling 768-cell strip, on-drawing column annotation and promoted legend, the lens whisper and the landing — built and self-verified 2026-08-23; not merged, not deployed
- [x] review arc 2b and arc 3, and merge `forward-pass` then `waterfall` to main — both reviewed, merged and deployed 2026-08-23 (live bundle verified)
- [x] arc 4 "melding streams" design exploration — closed 2026-08-24 with the a3 ruling: three static comps at 1280 from real data — (a) full canyon, (b) + slim block labels, (c) etchings are the machinery — built and sent to Bill 2026-08-23. His verdict: right direction, too busy (the reference photo was rough, not to be copied literally) — so the a/b/c pick is superseded by comp a2 "one clear story" (hero stream = the last token and only its landing throws the founts, strands only into it — five on this sentence, block 5 honestly none, calmer geometry, walls receded, one plate open, left-edge labels) — built, reviewed and sent to Bill 2026-08-23. Superseded by the Sol consult (2026-08-23): Bill briefed his Codex assistant as design consultant; it delivered proposal comps built from our own extract.json pipeline (memory-room + pinball "routed table" families, in design/arc4-comps/) plus 16 aesthetic studies (proposal-assets/). Bill's pick: the memory-room granular aesthetic ("stunning") — six full-width walls of real weight bytes the sentence falls THROUGH, calm at page scale, granular up close. Bill gave the word for comp-a3 (granular look on the data-qualified geometry) but the build was interrupted three times by an upstream API incident (2026-08-23 night) and paused on his call — no a3 files exist yet. comp-a3 built, reviewed and sent to Bill 2026-08-24 (design/arc4-comps/comp-a3.html/.png — self-contained SVG, no rasters: 9,216 byte-cell rects per the six walls, ~60,000 stream particles as dashed paths, all magnitudes recomputed from extract.json/filaments.json/fileFacts at build time). Bill ruled 2026-08-24: a3 is the one. Production build launched same day on branch `memory-room` (off main d026953): repaint ForwardMap.jsx to the a3 design, live — hero defaults to the last token and clicking any token re-aims the transfers, the landing stays the last position's alone, walls stay real in both modes, lens whisper removed (flagged), full invariant gauntlet before any merge. Brief: arc-4-production-brief in the project memory. Merge+deploy only on Bill's word after independent verification. After a3: Bill's ruling, then the production brief. Standing rulings absorbed: walls are frozen-blue (settled by the pick); full-sheet width assumed by the composition
- [x] arc 4 production build on branch `memory-room` (off main d026953): ForwardMap.jsx repainted to comp-a3 "memory room, granular", live on the reader's sentence — walls from `fileFacts` windows, granular filament streams, live head-choice rule and transfers, BLOCK/HEAD chips, dot-grid landing, in-screen legend that states every rule, lens whisper removed — built and self-verified 2026-08-24; not merged, not deployed
- [x] arc 4: independent verification (claim by claim, with controls) — done 2026-08-24. Every hard invariant held; thirteen findings, none a correctness blocker
- [x] arc 4 fix pass on `memory-room` 2026-08-24, six fixes, each re-measured: (1) THE SIGN WRAP — `wallWindow()` was reading the i8 c_attn window bytes unsigned, so byte 255 painted as the brightest cell when the manifest says it is the weight −1. Every wall's histogram was bimodal at the extremes and the middle-96% safeguard was a no-op (lo 0, hi 255 on all six). The bytes are now read signed and a cell's brightness is |weight|, which is the size of the weight and not its sign; the stretch now has something to do — 0→33 of a largest 47 on block 0, 0→27/43, 0→14/21, 0→23/33, 0→20/31, 0→13/19 down to block 5 — and the grouping is by magnitude, which also cuts a wall from ~57 paths to 44/38/22/32/29/19. The walls look different and Bill has the before/after zoom. Still byte-identical between illustrative and real, still untouched by a keystroke. (2) UNSCRIMMED LABELS — the register names and the wall plates were drawn *under* the fall; they are now drawn over it, each on a scrim of the screen's own ground sized from the line's own measured width (the old `length × size × 0.6` forgot letter-spacing, which on a 19-character name is a fifth of the line). `#mr-fade` also lacked `gradientUnits`, so every scrim squeezed the whole three-stop gradient into its own box instead of matching the ground at its y. Measured off the raster, glyph body α ≥ 0.8: `mr-tensor is-fine` 35.2% → 0.00% below AA at 1280 and 62.2% → 0.00% at 390; `mr-tensor` 20.0% → 0.00%; `mr-label` min 1.60 → 5.51; `mr-label is-dim` min 1.55 → 5.11; `mr-quiet` min 1.66 → 6.93; the aperture plate, which was a third too narrow for its own words, min 2.59 → 5.02; the green key line 15.0% → 0.00%. Nothing anywhere is below AA now, at either width, on the default sentence or a 21-token one: overall 0.84% → 0.000% at 1280, 1.99% → 0.000% at 390. (3) HERO ≠ LAST — with an early hero the hero's stream used to run to aperture depth at its own x while the aperture hung at the last token's, fed by a stream that had faded. The last position's stream is now the one that reaches the aperture, hero or not, dimmed when it is not; the hero's own fall ends in the mist with the rest, and the legend says that instead of "only the hero reaches it". (4) At around seventeen tokens a chip has room for two characters, which is a different word — chips now carry their position number below three characters of room, the drawing's top line says so, and the words stay reachable (hero named in the legend, transfer sources named at their callouts, full token in every tooltip and accessible name). (5) Dead exports, a stale comment, live arrays published to `__mapState`, two hard-coded whites. (6) These docs. The auto-head list on the default sentence is b0 h7, b1 h9, b2 h3, b3 h8, b4 h7, b5 h8 — the b2 h11 / b4 h3 pair in the arc-4 comp notes belongs to the comps' own sentence, not to this one
- [x] arc 4: the width question — Bill ruled full-bleed 2026-08-25. Instrument F breaks out of the essay's 760 px column and takes the sheet at 1280: the figure is 1,200 px wide and the drawing 1,166, which is a pixel to a drawing unit, exactly what comp-a3 was drawn at. Built and re-verified the same day (one commit on `memory-room`); not merged, not deployed
- [x] arc 4: captures to Bill (walls before/after, hero states, 21-token, both widths), then merge + deploy on his word only — Bill ruled 2026-08-25 (walls right, full-bleed, merge): ff-merged to main f3b5eee, deployed gh-pages a533933 "Deploy f3b5eee", live bundle index-D0_HfnwC.js verified with a cache-busted fetch, bundle 200. ARC 4 SHIPPED
- [ ] arc 5 "the slow pass" on branch `slow-pass` (off main cde1296), launched 2026-08-25 from Bill's review of the live memory room: the run is far too short (~1.1 s today) and the piece should work as an art piece the way his Chaos build does — so instrument F gets a narrated guided replay at reading speed (60–120 s, caption line with the pass's own numbers, play/pause/speed/step, reduced-motion gets a stepper), an ambient idle mode (the drawing runs itself, calm and honest, only while visible; wall cells never re-render), 2–3 motion-tempo variants behind a dev switch for Bill to pick from, and nothing dead on the sheet — carriers, landing rays and wall cells all answer a click with a plain-words readout, and the "no transfer" and UNEMBED plates explain themselves. Brief: arc-5-slow-pass-brief in the project memory. First builder stopped mid-arc 2026-08-25 (harness restart killed it) AFTER committing 8b1d93c — the tour (lib/tour.js, single source of the sheet's own sentences), the docent bar (play/pause/step/speed + reserved caption box, sticky over the tall drawing), the CSS-custom-property reveal (a stop moves the picture with zero React renders in the fall and zero wall-cell touches), ambient mode, and the three ?motion=a|b|c studies are IN; still owed: click-to-inspect (carriers, rays, wall cells), the self-explaining no-transfer/UNEMBED plates, docs, and the ENTIRE verification gauntlet (nothing measured yet). Resume: fresh builder with the brief + "gap-list 8b1d93c against the brief's deliverables, finish, self-verify". Then independent verify, captures + motion evidence to Bill, he picks a preset, fix pass strips losers, merge+deploy only on his word. RESUMED AND FINISHED 2026-08-26 (three commits on `slow-pass`, tree clean, not merged, not deployed). The gap-list against 8b1d93c: click-to-inspect and the two self-explaining plates turned out to be IN and working (the handoff's "still owed" was written without reading the commit's own message); what was genuinely unfinished was the whole verification gauntlet, the docs, and four faults measurement found. (1) THE CAPTION DID NOT FIT ITS BOX at the width most of this page is read at — the reservation was three lines at every width, and walking all 35 stops measured eleven captions losing a line at 900 px and eighteen losing one or two at 700, the essay column. It is now reserved per band, measured the legend's way with one line of margin over the worst stop on both the default sentence and a 21-token one: 4 lines on the sheet for 3 used, 5 in the column for 4, 6 in the narrow column for 5, 5 on the phone for 4. Zero stops clip at 390, 641, 799, 800, 1199 or 1280. (2) THE STOP COUNTER WALKED SIDEWAYS — right-aligned, and its wording changes on every stop, so every glyph in it moved: 0.000050 on opening the tour, 0.000149 on each step, and two thirds of the 0.000325 a keystroke measured. Same fault the arc-4 fix pass found in the status note, same fix: align left. (3) EIGHT CONTROLS WILL NOT SHARE 320 px with a stop counter — it was being squeezed to sixteen pixels and an ellipsis at the phone's width and now takes a reserved line of its own. (4) TWO INCOHERENT READOUTS — pressing a wall plate after clicking one of its cells turned the tensor off, left the cell's words standing and disabled OPEN IN THE FILE beside them; and a carrier readout naming the hero survived a re-aim, describing a carrier into a token that was no longer the hero and might no longer exist. Also: a cell readout called a signed i8 value "the byte", which contradicts the legend two inches above it, and now names both the quantised value and the byte the file stores
- [x] arc 5: independent verification — done 2026-08-27 (claim-by-claim, own harness, main cde1296 in a worktree as control). No correctness blockers: zero CLS everywhere incl. a full tour (with a pre-fix control at 065debc that catches the counter walk), height three viewBoxes only, walls digest- and node-identical to main through everything, 12 class writes per tour / 0 for ambient, decoding parity byte-identical, __mapCheck passes and can fail, 11 spoken numbers exact incl. a wall cell decoded from the raw base64. Corrections/refutations logged in the fix-pass item below
- [x] ! arc 5 fix pass — DONE 2026-09-01, eight commits on `slow-pass` (e9e08ee…81cbd95), tree clean, still not merged and not deployed. Bill ruled on the motion studies first: (a) measured docent wins. Its numbers are now the drawing's only ones, in one `MOTION` block in `lib/tour.js`, and everything that existed to choose between three tempos is gone — the `MOTION_PRESETS` map, `motionPresetId()`, the `?motion=` querystring read, the `is-motion-<id>` class, the two stylesheet rules that turned half the ambient loop off for preset c, and the two `buildTour` branches a never took. Preset a's timings are unchanged and measured: 35 stops and 107,800 ms at 1280, the same as before the strip.
  - (1) FRAME CLAIM — re-measured quiet on this machine, 60 s per run, rAF timestamps with the tour's own stop index sampled at every frame. A playing tour at 1× in real mode on the default sentence: 59.8 fps at 1280 (mean 16.74 ms, p99 16.8, max 150, ONE frame over 50 ms in sixty seconds and none over 100 apart from it) and 59.9 fps at 390 (mean 16.69, p99 16.8, max 33.4, zero over 50). Eighteen stop changes in each run, and the worst frame at a stop change was 33.3 ms at 1280 and 33.4 at 390 — one dropped frame, never two, and the single 150 ms frame at 1280 was NOT at a stop change. Ambient as the control: 60.0 fps, mean 16.67, p99 16.8, zero over 50 at both widths. So the verifier's 48–49 fps and ~465 ms worst frame were the loaded machine, and this branch's own earlier "57 fps, eleven frames over 50 ms, max 83 ms" was too; the Important bullet is corrected below. No recalc fix was needed and none was made.
  - (2) STOP COUNT — the count was already computed everywhere it is SPOKEN (the docent's counter, the line under it, the THE TOUR legend entry all read `stages.length`); the one written-down number was the caption for the stop where there is no landing to draw, which promised "top eight" at every width. It is handed the geometry's own `splashN` now and says the tallest 8 or the tallest 6. Measured after: 35 stops and 107.8 s at 1280, 900 and 700; 33 stops and 104.6 s at 390; 19 stops and 65.8 s illustrative at every width. Both counts are stated in Important and in the spec.
  - (3) WALL CELLS — a wall is now one tab stop with a roving cursor inside it: arrows move a cell at a time, Home and End run to the ends of a row, Enter or Space reads the cell through the same `say()` the click uses, Escape puts it away. The cursor IS the pointer's own `mr-hover` outline, written straight to the DOM by both hands, so no wall cell is re-rendered by either. Six new tab stops, not 1,536. Measured with a real Tab walk at 1280 in real mode: 57 focusable controls in instrument F against 51 before, every one of them labelled, all six wall grids reached; illustrative 41 against 35.
  - (4) AMBIENT AND SCROLLING — a wheel, a scroll and a touch drag now reset the idle clock, all three as passive capturing listeners, and the reset is a functional state update so the common case bails out of React without a render. Measured at 1280, real mode, F in view: 30 s of continuous scrolling and ambient never starts; on the build before the fix it had started mid-scroll and was still running at the end of the same 30 s. Stop scrolling and it starts at the first poll past twenty seconds (21 s).
  - (5) NITS — focusable count was 51, not 49 (the verifier was right); it is 57 now. The commits after 8b1d93c were seven before this fix pass (c7236aa, 065debc, 84685fe, 11faabc, f689546, f1ec5b0, 2e28088) and fifteen including it — "three commits" in the arc-5 item above counts only the resuming session's own. The tour is 107,800 ms, which is the 1 min 48 s the docs already say; illustrative is 65,800 ms against the docs' 66 s. Both stand.
  - PLUS three findings from Bill's own review of the live sheet, folded into the same pass. (A) THE ANSWER APPEARS WHERE THE CLICK WAS — he clicked a wall cell and said no information came up. It had: the sentence went to the readout row under the whole figure, two screens below the wall at 1280, and the only thing near his pointer was the browser's tooltip for the WHOLE tensor, a second late. Hovering a cell now opens a card inside the SVG beside it; clicking a cell, a carrier, a ray or either plate pins one there with a leader back to the mark, and the full sentence still goes to the row. The card prints a LEAD out of `lib/tour.js` — the first half of the row's own sentence — so card, row and tour cannot disagree. The wall plate's `<title>` is gone. Also from his printout: the readout row explains itself when nothing is chosen (it said "click any steel box", which is the language of two drawings ago), and the two greyed buttons say what they open and what would make them open, with OPEN THE INSTRUMENT in place of OPEN —. (B) THE DOCENT'S LINE FOLLOWS THE CUE — the current stop's lead also stands on the drawing beside the thing it describes, one fixed slot per kind of stop, stepping and reduced motion included. (C) THE DRAWING SAYS FIXTURE AND PART — the purpose line, the token strip's label, the tour's opening stop and the five legend headings carry the essay's own analogy, with the water and the light left underneath as flavour. (D) INSTRUMENT E's BYTE BAR says its one invitation, on its own reserved line under the bar: click a piece of the bar to choose that tensor.
- [ ] arc 5: captures + live ?motion=a|b|c to Bill for the preset ruling — package sent 2026-08-27; preset c presented honestly (ambient below visibility: 0% of pixels differ between its first and middle frames; verifier says don't offer it as an ambient option as-is), preset b labelled as deliberately over the 60–120 s target (173 s); then fix pass, then merge + deploy only on Bill's word
- [ ] arc 6 candidate "the veins" (interpretability): per the assessed research brief (design/interpretability-research-brief.md) — Option A feature-card instrument (real gpt2-small-res-jb features baked at build time, loud sibling-model label, auto-labels tagged as model-generated) + the circuit case-studies prose section (Dallas→Austin, poetry planning, the wrong arithmetic self-explanation). Option B (live features) chained to the tabled GPT-2-small model upgrade, not standalone; Option C (train our own SAE) out of scope as its own project. Not ruled yet
- [ ] arc 6 candidate "the loop and the landing" (agents + honest limits): prose section (+ one house-style drawing) on how a frozen model becomes an agent — the loop around the fixture, context as the moving part, memory as files the loop writes ("the fixture never learns; the harness takes notes") — plus the always-answers blurb: the pass is a total function, silence is not in the vocabulary, helpfulness training shapes tone but adds no abstain mechanism, and the machine's real certainty is the SHAPE of F's landing (spiky = sure, flat = lost). Brain parallels only with the contrasts stated (population codes yes, but brains rewire continuously and the fixture never does). Not ruled yet
- [ ] arc 6 candidate "the contrast" (Bill, 2026-09-01, from reading the live page): the human rewrite and the machine's exact re-read are both on the page but never in the same place — 05 has the rat and the check-out-for-edit callout, E in 01 has the live byte-for-byte proof, 09 has the one side-by-side line. Proposed: a two-card block in section 07 under the "dinner in Lisbon" paragraph — the brain: coordinates into a model that has changed since, so the render drifts; the file: the same bytes through a model unchanged since the page was built, read again just now, E's fingerprint printed live. Machine side demonstrated, brain side cited (it cannot be demonstrated honestly). Wording is Bill's to write or approve; must keep 09's line that the model's KNOWLEDGE is reconstructed and confabulates — only the file and the context are recalled exactly. Not ruled
- [x] deployed to GitHub Pages 2026-08-20: https://bill-viking.github.io/fixture-and-part/ — link this from Confluence (README documents the `VITE_BASE` env var needed); link from Confluence for Blue Origin teammates — their network must allow huggingface.co for real mode, otherwise the page stays illustrative with a muted notice

## Important
- Colour law is the visual thesis, not decoration: frozen = machinery (weights/dies), moving = the moving part (activations/tokens), keys = searchable metadata — don't swap this mapping. The tokens are named for those roles, so the colours filling them can be repainted (they have been, once) without touching the law.
- Zero layout shift is a standing invariant, and it is measured rather than assumed — CLS via a buffered PerformanceObserver, plus a geometry diff before and after the model load, because a shift below the fold does not register as CLS. The diff is over the page's structural boxes — the header, every section, every instrument slot and the foot — and not over every element in the document, so it catches a block that moves and would not catch a word that shifts inside one. Two shifts are known and accepted rather than fixed:
  - Instrument A's token strip reflows when the real model lands and the word-split pills are replaced by BPE pieces (~0.058 at 1280, and only while instrument A is on screen). The pills are genuinely different words of genuinely different widths; the strip's own height is already reserved, so nothing outside it moves. Inherent, pre-existing, and measured identically on the palette before this one.
  - The cache counter in instrument B reports ~2e-6 on the STEP that takes the entry count from one digit to two. The number's box is reserved and does not move; the digits inside it do.
  - Typing measures a hairline — 3.1e-5 at 1280, 6.8e-5 at 390 — from one text node re-wrapping in the prose that quotes the reader's sentence. Measured identically on main to the last digit, so it predates instrument F and the waterfall; it is a sub-pixel reflow of a curly quote, not anything moving.
- The walls are drawn from signed i8. `attn.c_attn.weight` is stored i8 at zero point 0, so a byte above 127 is a negative weight and 255 is −1; read unsigned, a wall paints the sign bit and every window's byte histogram is bimodal at the extremes, which is what the first build did. Brightness is the weight's magnitude — its size, not its sign, because both signs are the machinery working — and the middle-96% stretch is measured on the magnitudes, where it does real work: 0→33 of a largest 47 on block 0 down to 0→13 of 19 on block 5. Anything that reads those bytes again should read them signed.
- Perf framing, from the independent verification: like-for-like first paint favours this branch (116–124 ms against main's 216 ms); the FCP delta is +32 ms. The "332 vs 216" pairing in the production builder's own report compared two different things and should not be quoted.
  - Instrument F adds no third one, before the repaint or after it. On `memory-room`, model load, RUN THE PASS, a token click either way, BLOCK and HEAD chips, a wall click, OPEN IN THE FILE, STEP×2, the decoding toggle and the mode toggle all measure 0.000000 at both 1280 and 390, and the structural-box geometry diff across the model load moves nothing inside instrument F at either width (every mover is another instrument's own content: A's pills, B's candidate rows, C's query tokens, D's lens bars). The observer is proved rather than trusted — a 40 px block inserted into the instrument body in view reads 0.0128 at 1280 and 0.0252 at 390. Re-measured after the fix pass, with the plates and the register names moved above the fall: still 0.000000 for the model load, every token click, both chip rows, a wall click either way, RUN THE PASS and a token click at 21 tokens, at both widths, positive control 0.0143 / 0.0325. The one non-zero is the pre-existing prose reflow and it is identical on the branch before the fixes and after: one keystroke measures 0.000031 at 1280 and 0.000087 at 390, and it comes from `p.inst-reading`, the sentence quoted above the drawing, not from anything in the figure. Two shifts the old drawing had were fixed rather than accepted — a control swapping for a ghost line and moving the button beside it, and a right-aligned status note whose glyphs walked sideways whenever the wording changed length. Both are the same failure and have the same fix: pin the box, align the text left. Re-measured again after the full-bleed pass (2026-08-25), with the same harness run against the branch before it as a control: model load, three token clicks, both chip rows, a wall click either way, RUN THE PASS, STEP twice and a token click at 21 tokens are all still 0.000000 at both widths, the geometry diff across the model load still moves nothing inside F at either width (viewBox identical across the load), and 390 is identical to the control on every line including its positive control (0.0325). Two numbers at 1280 differ from the control and neither is a new shift. The prose hairline is now scored over a wider box, because the reading line it comes from is 1,166 px rather than 678: the same sub-pixel reflow reads 0.000114 for a keystroke where the control reads 0.000066, and 0.004888 against 0.002842 for replacing the whole sentence — one source, its box unchanged (`301,126,678,18` on the control and `57,126,1166,18` here, from == to in both), and the ratio is exactly 1166/678. CLS is area-weighted, so any element that gets wider scores the same movement higher; that is the cost of the ruling and not a regression in what moves. The other is the decoding toggle in instrument B, which reads 0.000695 when B itself is parked in view — byte-identical on the branch before this change, so it is B's own and predates the breakout. The earlier 0.000000 for it was measured with F parked, where B's shift sits off screen and is not reported.
- Instrument F is drawn in one law with three settings of it, and what a setting really chooses is how many pixels sit under a drawing unit. The drawing is 1,166 units wide at every width; the phone (≤ 640 px) renders it at about 342 px, so a unit is 0.29 px and the type is set roughly twice the column's; the column (641–1199 px) renders it at 678, 0.58 px a unit; the sheet (≥ 1200 px) renders it at 1,166, one pixel a unit, which is what comp-a3 was drawn at and where the type and the grain go back to the comp's own sizes. The stylesheet's breakout query and `FULL_MQ` in ForwardMap.jsx are the same 1,200 px and have to be changed together — if they ever disagree the drawing is laid out at the wrong scale. The rendered type barely moves across that boundary (a legend line is 9.6 px at 1199 and 10.1 px at 1200), which is the point: the drawing nearly doubles and the words stay the size they were.
- The height of instrument F's drawing is a function of its breakpoint and of nothing else — not of how many tokens are in the sequence, not of which register is open, not of whether a model has loaded. Measured across 0, 1, 8 and 21 tokens in both modes, with a register open and with an early hero: the viewBox is 1166 × 2238.75 at 1280 (rendered 1166 × 2238.75 px — a unit is a pixel there), 1166 × 2926.725 in the column setting between 641 and 1199 px, and 1166 × 4919 at 390 (rendered 320 × 1349.97). Two things pay for that. The legend prints live numbers, so each of its four entries has a reserved line count and clips rather than reflows (measured usage against the reservation: 6/7, 6/7, 8/9, 5/6 at 1280 and 7/8, 9/9, 13/14, 6/7 at 390 across illustrative, real, a long sentence and a two-token one; the column setting's was 8/9, 9/10, 12/13, 7/8). And the number of filaments a stream is drawn with steps with the token count — 64 × 12 dimensions up to twelve tokens, 32 × 24 to twenty-four, 16 × 48 beyond — which is stated on the drawing and also holds the element count roughly flat.
- Arc 5's own standing rules, first measured 2026-08-26 on branch `slow-pass` at 065debc/84685fe and RE-MEASURED IN FULL after the fix pass 2026-09-01 at 81cbd95, with main cde1296 in a worktree as the control and, where a fault had to be shown, the pre-fix commit 41bfcf9 in a second one:
  - **The drawing is taller, at every breakpoint, and still a function of the breakpoint alone.** The legend gained a fifth entry (THE TOUR), so the viewBox is 1166 x 2336.95 at 1280 against main's 2238.75, 1166 x 3121.525 in the column setting, and 1166 x 5217.1 at 390 against main's 4919. The fix pass added NOTHING to any of those: the three cards live inside the SVG at a fixed viewBox, and the legend's new headings are gutter lines beside bodies that already reserved more lines than a heading uses. Re-measured after it, one height per breakpoint held across twenty-four states each — both modes × (0 / 2 / 8 / 21 tokens, five stops of a played tour with the docent card on the drawing, a register open, a pinned card, the sheet running itself) — plus six more runs that waited out the idle timer to catch ambient genuinely running. One distinct viewBox at 1280, one at 900, one at 390.
  - **The caption's reservation is measured, not guessed, and it is per band.** The caption prints live numbers and changes on a timer, so it clips rather than reflows, exactly as the legend does. Walk every stop of a real tour at a width and take the tallest caption, on the default sentence and on a 21-token one, then reserve one line more. Anything that changes the tour's wording has to re-measure this, and the fix pass changed the opening stop's wording, so it was. Counted as LINE BOXES rather than as scrollHeight — a clipped box's scrollHeight can never read below its own height, so scrollHeight cannot see a caption that is shorter than its reservation, which is how an earlier pass came to believe it was measuring one: 3 lines used of 4 reserved at 1280, 4 of 5 at 1199 and at 800, 6 of 6 at 641, 4 of 5 at 390 (where the caption is the stop's lead alone). Band for band, identical to the same walk on the branch before the wording changed. Zero stops clip at any of those widths. Two corrections to what this bullet used to say: the worst caption at 641 uses SIX lines of the six reserved, not five, so that band has no spare line and the reserve-one-more rule is not met there — pre-existing, not introduced, and nothing clips; and the worst stop is `sentence` at 1280/1199/800 and `tail` at 641.
  - **The cards are reserved the same way.** Three of them are drawn and they are one shape: the pointer's, which follows the wall cell under it; the pinned one, which a click leaves beside what it clicked with a leader back to it; and the docent's, which stands beside whatever the tour is talking about. All three print a LEAD out of `lib/tour.js` — the first half of the sentence the readout row prints in full — so a card, the row and the tour cannot say different things about the same mark. `__cardLines()` wraps every lead the sheet can produce (all 9,216 wall cells, every carrier, every ray, both plates, every stop of the tour) into the band's own column count and takes the tallest, on the default sentence and on a 21-token one. Click card: 70 columns and 5 lines used at the sheet, 45 and 7 in the column setting, 52 and 6 on the phone — reserved 6, 8, 7. Docent card: 3, 4, 3 used — reserved 4, 5, 4. Nothing clips at 1280, 1199, 800, 641 or 390.
  - **The docent's card has one fixed slot per kind of stop, and it never stands on words.** The sentence and the rim beside the RIM label; a block's fall, head, transfer and silent stops on that block's own wall, starting where its BLOCK/HEAD label's scrim ends, so the card never has to be pushed off the band to clear the label; ln_f and the unembedding at the aperture; the landing at the FAR end of the bars, because the tallest bar is the first one and a card beside it would stand on the one thing that stop is about. On the phone the top of the drawing has no clear strip at all — the chips, the rim label and the first block's callouts fill every unit of it — so there the sentence and the rim take the head of the first wall. The list of boxes a card may not open over is the six KEY callouts plus every BLOCK/HEAD label, every tensor name and window spec, the RIM label, the aperture plate, the landing title and the token strip, and the placement tries its candidates against that list and then nudges. Measured by walking every stop: 35 at 1280, 35 at 800, 33 at 390, on the default sentence and on a 21-token one — 214 stops, zero overlapping any of those boxes, zero clipped, zero outside the drawing's bounds. One real change was needed to get there: the phone's card padding is the column setting's 9 units rather than 14, because the clear window inside a wall band at that width is 187 units (the block below reaches a line of callout type up into it) and a four-line card at the old padding was 193.
  - **A stop moves the picture with almost no DOM work, and none of it in the drawing.** Measured with a MutationObserver over the whole SVG. A full 35-stop tour writes twelve `class` attributes on the six register groups as the cue moves — and, since the fix pass gave the docent a card on the sheet, 60 to 67 more attributes on that ONE card as it moves from slot to slot, plus a handful of text nodes inside its own `<text>` elements: 72 writes in illustrative mode and 79 in real, at both widths. Fifty-four seconds of the sheet running itself writes two attributes, both of them putting the pointer's own outline and card away, and neither of them from the ambient loop. Nothing else in the SVG is written at all and not one of the 9,216 wall cells is touched by any of it. A hover sweep and the keyboard cursor write only the one outline rect and the one hover card — 827 to 940 attribute writes for 24 pointer moves, a click and four keystrokes, all of them on those two elements. That is what the five custom properties buy, and it is the reason a tour can be minutes long.
  - **The tour's length is a fact about the screen as well as the sentence.** The landing is counted one bar at a time and the phone draws six bars where the wider settings draw eight, so a real tour on the default sentence is 35 stops and 107,800 ms above 640 px and 33 stops and 104,600 ms at or below it; illustrative is 19 stops and 65,800 ms at every width, because with no pass the landing is one caption rather than a stop per bar. Nothing on the sheet states that count as a constant — every place that speaks it reads `stages.length` — and the one caption that says how many bars are coming is handed the geometry's own `splashN`. The docs used to say 35 flat and that caption used to promise "top eight" at every width.
  - **The ambient loop is free and a stop costs at most one dropped frame.** CORRECTED 2026-09-01: the "57 fps, eleven frames over 50 ms, max 83 ms" this bullet used to carry was a loaded machine, and so was the independent verifier's harsher 48–49 fps with a ~465 ms worst frame. Re-measured quiet, 60 s per run, rAF timestamps with the tour's own stop index sampled at every frame, real mode, default sentence, 1×. Ambient, as the control: 60.0 fps, mean 16.67 ms, p99 16.8 ms, zero frames over 50 ms at 1280 and at 390, both before the fix pass and after it. A playing tour BEFORE the fix pass: 59.8 fps at 1280 (mean 16.74, p99 16.8, max 150 ms, one frame over 50 in sixty seconds and it was not at a stop change) and 59.9 fps at 390 (mean 16.69, p99 16.8, max 33.4, zero over 50); eighteen stop changes per run, worst frame at a stop change 33.3 ms and 33.4 ms — one dropped frame, never two. AFTER the fix pass, with the docent's card now moving on the sheet at every stop, two runs: 59.5 then 59.9 fps at 1280 (mean 16.80 / 16.68, p99 16.8 both, max 66.7 then 33.4 ms, three frames over 50 in the first run and none in the second) and 59.4 then 59.9 fps at 390 (mean 16.83 / 16.70, max 50.1 then 33.4, one over 50 then none). ZERO frames over 100 ms in any run at either width, before or after. The two runs disagreeing about a handful of 50-60 ms frames is run-to-run noise on this machine rather than a cost of the card — the second run is indistinguishable from the pre-fix baseline. No recalculation fix was needed and none was made.
  - **The walls are still the deployed page's own bytes.** A digest over every wall cell's path data and opacity — 184 magnitude-group paths a wall set, an FNV-1a over `d` plus `opacity` in document order — is 3069398707 at 1280 and 1842662551 at 390, identical on main cde1296 in a worktree and on this branch after the fix pass, identical between illustrative and real, and unchanged by a whole tour, by the cards, by the keyboard cursor, by a keystroke and by ambient, with the very same DOM nodes each time (identity, not just equality: every node is stamped before and checked after). Those two numbers are this harness's own hash; the 3094013159 / 2617863751 pair recorded earlier came from a different hash function over the same paths, and the claim either way is that the branch and main agree.
  - **The walls answer the keyboard now, and everything in the instrument is labelled.** A wall is one tab stop with a roving cursor inside it rather than 1,536 tab stops or none. Measured with a real Tab walk at 1280 in real mode on the default sentence: 57 focusable controls in instrument F, every one of them with an accessible name, all 57 reached by Tab, and all six wall grids among them. Before the fix pass it was 51 — which is what the independent verifier counted, and the 49 this file used to say was wrong. Illustrative is 41 against 35, the difference being the load button plus the carriers, rays and silent plate a pass has not drawn yet.
  - **Zero CLS, everywhere the fix pass touched and everywhere it did not.** Re-measured 2026-09-01 at both widths in both modes: idle, play, pause, both steps, all three speeds, a whole tour played end to end, a 24-move hover sweep across a wall, a wall-cell click, a wall plate, focusing a wall grid, eight arrow keys of the cursor, Enter, Escape, a carrier, a ray, the no-transfer plate, the UNEMBED plate, a BLOCK chip, a HEAD chip, a token re-aim, a keystroke, and a segment click on instrument E's byte bar. Every one of them 0.000000 at 1280 and at 390, in real mode and illustrative. Positive control 0.008424 at 1280 and 0.019330 at 390.
    Two traps, both of them real and both of them worth writing down. A buffered PerformanceObserver delivers the page's own load-time shifts in a LATER task, so the first measurement after arming is contaminated unless the flush is drained first — that is where an earlier "model load 0.000795" came from. And Chrome's scroll anchoring absorbs a block inserted above the viewport's anchor by adjusting the scroll instead of moving anything: the same 40 px probe reads 0.000000 when it is inserted while the page is scrolled and 0.008424 when it is inserted at scroll offset zero, where there is nothing above the viewport to anchor to. A positive control that lands in the first case is measuring the scroller and proves nothing. Because scroll anchoring can hide a shift from the observer, every action was ALSO scored by a diff of where the page's structural blocks actually are, in page coordinates, before and after — sticky boxes excluded, since a sticky box's page position changes with the scroll by design. Nothing moved for any action except the positive control, which moved everything below it by exactly 40 px.
  - **An answer opens where the eye already is.** The row under the drawing still carries the full sentence, but the first half of it — the LEAD — now opens as a card ON the sheet beside the mark it is about, because a reader who clicks a wall cell at 1280 is two screens above the row and sees nothing happen. Hovering a cell opens one at the cell; clicking a cell, a carrier, a landing ray, the no-transfer plate or the UNEMBED plate pins one with a short leader back to the mark; one pin at a time, the next click replaces it, and Escape or a press on the screen's own empty air takes the card and the row down together. The wall plate's whole-tensor `<title>` is gone with it: a browser tooltip naming the WHOLE tensor, a second late, over a wall whose every cell has its own answer, is what made the sheet look as though information showed up on some cells and not others. And the row says something when nothing is chosen: "no tensor selected — click any steel box" was the language of two drawings ago (the machinery has not been steel boxes since arc 4), and both buttons beside it now say what they open and what would make them open, with OPEN THE INSTRUMENT in place of OPEN —. Instrument E's byte bar got the same treatment for the same reason: Bill read its selected segment as a slider handle and tried to drag it, and one static reserved line under the bar now says "click a piece of the bar to choose that tensor".
  - **The drawing says the essay's own analogy.** Section 02 is called "Weights are the tooling. Activations are the workpiece" and the drawing under it named neither half. The purpose line, the token strip's label (THE PART — YOUR SENTENCE, ONE STREAM EACH), the tour's opening stop and the five legend headings now do — THE FIXTURE over THE WALLS, THE PART over THE FALL, THE TOOLING / TOUCHES THE PART over THE TRANSFERS — with the essay's word set back in the legend's grey. The headings are gutter LINES rather than one string because the gutter is 188 drawing units and holds sixteen characters of the column setting's type; measured at 1280, 1199, 800, 641 and 390 every line fits, the widest (TOUCHES THE PART) at 185 of 188, and they cost no height because every entry's body already reserves more lines than its heading uses. The water and the light stay underneath as flavour. The essay's prose is untouched — `git diff main -- src/content/essay.js` is empty — and so is `fileFacts.json`, byte for byte.
  - **The motion studies are gone.** Bill ruled 2026-09-01: (a) measured docent. Its numbers are the drawing's only ones now, in one `MOTION` block in `lib/tour.js` — fade 620 ms, 0.72 of a fall stop spent travelling, and the ambient sweep/glint/carrier/breathe/hero timings a had. The `MOTION_PRESETS` map, `motionPresetId()`, the `?motion=` querystring read, the `is-motion-<id>` class on the screen, the two stylesheet rules that turned half the ambient loop off for preset c, and the two `buildTour` branches a never took (a block as one stop, the landing arriving all at once) are all removed, and there is no dev switch left on the sheet. Preset a's timings are unchanged, measured before and after: 35 stops and 107,800 ms at 1280 in real mode on the default sentence.
  - **Two pre-existing faults this pass found and did NOT fix**, both of them identical on the commit before it (41bfcf9) and so not introduced here. The THE TOUR legend entry clips its last line — "…it redraws nothing and…" — at 1199, 800, 641 and 390, because that entry's reserved line count is one short of what its own wording needs; unclipping it means giving the legend another line, which changes the drawing's height at every breakpoint and therefore the three viewBoxes this file quotes, so it is Bill's call rather than a fix pass's. And the caption's 641 band uses all six of its six reserved lines, which meets the box but not the reserve-one-more rule the band was built under.
  - **Contrast holds in all three states, cards included.** Raster method, glyph body alpha >= 0.8 — render the figure twice at one scroll position, once as it is and once with every word in the drawing hidden, then solve each touched pixel for its own coverage of the fill composited at the element's own opacity over the very ground under it. Re-measured 2026-09-01: 0.0000% below AA over 34,623 glyph-body pixels at 1280 and 8,619 at 390, idle, mid-tour and with the sheet running itself — 130,398 scored pixels in all, none of them below 4.5:1. Worst class floor 5.18 (`mr-tensor is-fine` at 390), unchanged; the new card text `mr-card-line` floors at 7.54 and the legend's new set-back analogy line `mr-leg-key is-role` at 8.62. The ambient sweep is painted before the words and every word stands on its own scrim, so the sweep never becomes a glyph's ground — and to measure it at all the sweep has to be frozen, because the two renders must share one ground and the sweep moves.
  - **The decoding paths are still byte-untouched.** Ten STEPs under each rule at 1280 in real mode on the default sentence, against main cde1296 in a worktree: greedy and the seeded sampled draw produce the same tokens, step for step, on both. Greedy runs "␣The ␣engine ␣was ␣still ␣on ␣the ␣ground . ␣The ␣engine"; sampled runs "␣The ␣next ␣one ␣could ␣be ␣the ␣very ␣first ␣to ␣get".
  - **The numbers on the cards are the pass's own.** `__mapCheck()` passes against a pass it runs itself — worst norm delta 0, worst filament delta 0 over 4,032 bins, worst transfer delta 0, and it reports the comparison pass as fresh — and it can fail: nudging one published norm by 1 makes the worst norm delta 1. Three card numbers checked by another route: a carrier card's 0.1662 against `__mapState.transfers[0].w` = 0.16624081134796143; a ray card's 8.0% against `__mapState.landing[1].p` = 0.0803706038325091; and a wall cell decoded from the raw base64 window in `fileFacts.json` — `transformer.h.1.attn.c_attn.weight_quantized`, the byte at row 3, column 7 of the 24 x 64 window is 232, which is the quantised value -24, and with the manifest's own scale 0.01448342390358448 at zero point 0 that is the weight -0.34760. The card, reached by keyboard, says "[3, 7] ... quantised value -24, which the file stores as the byte 232 ... 0.0145 · (-24 - 0) = -0.3476".
- Phase 1's numbers are explicitly heuristic, not real model output — keep that labelling intact if Phase 2 stays unmerged, so the two modes are never confused.
- Text drawn over a texture cannot be checked from the stylesheet. Instrument F's steel boxes carry a tensor thumbnail under their words, so the ground beneath a glyph is whatever that tensor's bytes happen to be there; contrast has to be measured off the raster (render twice, once with the text hidden, and score every glyph pixel against the pixel underneath it). Doing that found what a stylesheet sweep could not: the selected box's name line had dropped to 4.41:1, below AA across 99.4% of the glyph body. A naive `rgb()` regex is the other half of the same trap — anything built with `color-mix()` resolves to `color(srgb …)` and is skipped silently.
- The raster contrast method needs one more correction than it looks: a diff of the two renders finds every pixel the text touched, but most of those are the anti-aliased fringe, which is a blend of ink and ground by construction and scores near 1:1 whatever the design does. Solve for the coverage instead — the fill composited at the element's own opacity over the very ground under that pixel — and score only the pixels the glyph body owns. On the memory room that turns a meaningless 5.34% "below AA" into a real 0.14% (172 glyph pixels of 119,081 at 1280 real; 0.21% at 390; 0.04% at 1280 illustrative). It also found four real faults the stylesheet could not see, all of them low-opacity blue labels sitting on lit byte cells: the HEAD line at 3.73:1, the tensor name at 3.92:1, the window spec at 2.37:1 and the silent-register line at 1.51:1 median. Fixed by raising three opacities and by scrimming the wall behind a label in the screen's own ground. The threshold that word "body" means is α ≥ 0.8, and it has to be said out loud: the same frames scored at α ≥ 0.6 pull in the fringe band, where solving for the ink divides by a small coverage and the answer is mostly quantisation noise — the branch before the full-bleed pass reads 0.391% "below AA" that way and 0.000% at 0.8, on identical pixels. Re-measured at the full-bleed type sizes (2026-08-25, real mode, default sentence): 0.000% below AA at 1280 over 36,391 glyph-body pixels and 0.000% at 390 over 14,146, against 0.000% over 34,786 for the control at 1280, with every class's floor within a tenth of the control's (worst row `mr-tensor is-fine`, 4.79 → 4.75; the green key line 5.13 → 5.07).
- Instrument F draws no text over the picture without standing the picture back behind it. The wall dims to 55% under a stream and to 8% under a carrier, a label or a plate — and the dimming is a scrim painted in the screen's own background gradient rather than a per-cell alpha, which keeps all 9,216 wall cells out of every re-render. The scrim is close to what a per-cell alpha would compose to but not identical, and it is not claimed to be: the gradient is painted in the drawing's own coordinates, so a scrim is the ground at its own y, and what a cell keeps under it is 8% of a cell that is itself sitting on that ground. Measured off the raster, the ground under a scrimmed label reads 6–12 out of 255 per channel — the near-black of the screen — where an unscrimmed one read a lit byte cell. The other half of the rule is paint order: the words that name the drawing's parts are drawn after the fall, not before it, and each stands on a scrim sized from its own measured line width. The walls are memoised on the geometry alone: a keystroke redraws the streams, the carriers and the landing, and does not touch a single byte cell.
- Instrument F's lens whisper was removed in the arc 4 repaint. The drawing now says what the machine settled on at the landing instead, and instrument D still presents the same seven depths as a table, which is where they can actually be read. `src/lib/tensorTexture.js` and the `thumbnails` block of `fileFacts.json` are left in place but are no longer drawn by anything — dead weight to clear when something else touches that file. The thumbnails are ~70.8 kB, 32% of the shipped `fileFacts` chunk, and they still ship: clearing them means regenerating `fileFacts.json`, and the file is deliberately byte-identical to main for the whole of arc 4 so that the walls can be proved to be the same bytes the deployed page already reads. Deferred on purpose, not overlooked.
- **The human-brain comparison is load-bearing and permanent (Bill, 2026-09-01).** The essay's philosophical frame — sections 05–10: the rewrite, forgetting as a feature, the brain storing the fixture not the parts, perception as the same trick, convergence, the charioteer — is a critical key part of this program, not garnish around the instruments: it is what lets a reader (and Bill) understand how the machine and the brain relate. It is never cut, thinned or refactored; the prose stays verbatim from `the-fixture-and-the-part.html` (repo root; `src/content/essay.js` carries all 10 sections). New arcs may ADD to the comparison (e.g. "the contrast") and never subtract from it. The ideas from the founding discussions live in that essay; any new comparison material is written or approved by Bill.
