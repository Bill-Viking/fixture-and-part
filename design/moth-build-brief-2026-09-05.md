# BUILD BRIEF — the moth callout + Astra fixes (1)–(4) · 2026-09-05

Handed to an Opus builder working in a git worktree on branch `the-moth` off main (22015cf or later). Verified by the lane session independently; merged and deployed only on Bill's word. Same text as the builder's prompt; kept here for the record.

Scope, in one line: one approved callout added to section 08 of the essay, four fixes from Astra's review of 2026-09-05 (findings file `openai/ASTRA-FINDINGS-FP-2026-09-05.md`), nothing else. No STATUS/README/design/openai edits, no deploy, no installs, `npm run lint` and `npm run build` clean at the end.

1. Callout in `src/content/essay.js`, section '08', after its one `p` block: `{ type: 'callout', variant: 'warm', label: 'SPECIMEN — THE MOTH', html: <the approved text, entity style &rsquo; &ldquo; &rdquo; &hellip;, literal em dashes> }` — text in the moth note under "The words".
2. F narrates the LIVE decoding mode (Astra 1): `ForwardMap.jsx` imports the static `DECODING` (line 37) and passes `decoding: DECODING` (~1597); the landing sentences (~2436/2443), the legend plate (~3450) and `tour.js` (~147/149/447/479) always say "the sampler took" with temperature/top-k/seed. App holds the mode in `decode` (App.jsx:142) and passes it to B (line 580) but not to F (mount at 615). Outcome: greedy says greedy and prints no sampler settings; sampled as now; "same input → same trace, every time" qualified for sampled (same seed AND same step) or the "every time" dropped. tour.js stays the single source of the sheet's sentences. Mode flows as a prop.
3. STEP during the debounce (Astra 4): App.jsx:403 `canStep` trusts `runReady` while `text` already differs from the run's text. Gate canStep, the shortlist and the commit on the run belonging to the current text (F/E already compare `realBase.text === text`).
4. Reduced-motion scroll (Astra 5): App.jsx:92 `scrollToInstrument` uses `behavior: 'smooth'` unconditionally; use `'auto'` under `prefers-reduced-motion: reduce`; same for any other smooth scroll in src.
5. Stepper caption (Astra 2, words approved with the build): Stepper.jsx:445 TeachPair `b`: replace "every new token scans the whole rack, and racked rows never change." with "every new token scans the whole rack. In a real deployment the racked rows are stored once and never change; this page re-runs the whole sentence each step, and the file&rsquo;s own rounding can nudge an old row by a hair." Keep the greedy/sampled sentence after it. Leave `a` (illustrative has no quantization drift). Report how B sources its displayed rows.

Gauntlet: lint clean vs main, build passes with bundle size vs main, dev-server smoke in both modes with no console errors, callout present in 08, greedy/sampled narration verified by toggling in B and reading F's landing sentence and legend plate. Report file-by-file changes, commits, check results verbatim, and anything not verified.
